@model List<Employee>
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<table class="table table-bordered table-hover" style="background-color:aqua , black">
    <a href="New"></a>

    <tr>
        <td>
            <a href="/Employee/AddNew"> AddNew</a>
        </td>
    </tr>
    <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Address</th>
        <th>ImageUrl</th>
        <th>JobTitle</th>
        
    </tr>


    @foreach (var item in Model)
    {

        <tr>
            <td>@item.Id</td>
            <td>@item.Name</td>
            <td>@item.Address</td>
            <td>@item.ImageUrl</td>
            <td>@item.JobTitle</td>

            <td>
                <a href="/Employee/Edit/@item.Id"> Edit </a> 
            </td>

            <td>
                <a href="/Employee/Details/@item.Id"> Details</a>
              @*   @Html.ActionLink("_Details", "Employee", "Details", new{id = Model.id} ) *@
            </td>


            <td>
                <a asp-controller="Employee" asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-primary" title="Details">
                    <i class="bi bi-list-ul"></i>
                </a>
               @*  <a asp-controller="Employee" asp-action="GetEditView" asp-route-id="@item.Id" class="btn btn-outline-secondary" title="Edit">
                    <i class="bi bi-pencil-square"></i>
                </a>
                <a asp-controller="Employee" asp-action="GetDeleteView" asp-route-id="@item.Id" class="btn btn-outline-danger" title="Delete">
                    <i class="bi bi-trash"></i>
                </a> *@
            </td>




           @*  <td> 
            <a asp-controller="Employee" asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-primary" title="Details">
                <i class="bi bi-list-ul"></i>
            </a>
            </td> *@
           

        </tr>

       
    }
   @*  @foreach (var employee in Model)
    {
        <tr>
            <td>@Html.DisplayFor(m => employee.Id)</td>
            <td>@Html.DisplayFor(m => employee.Name)</td>
            <td>@Html.DisplayFor(m => employee.JobTitle)</td>
            <td>@Html.DisplayFor(m => employee.Salary)</td>
        </tr>

       

        <a asp-controller="Employee" asp-action="index" asp-route-id="@employee. Id" class="btn btn-outline-primary"
           title="Details"> <i class="bi bi-list-ut"></i> </a>
        <a asp-controller="Employee" asp-action="Index" asp-route-id="@employee.Id" class="btn btn-outline-secondary"
           title="Edit"> <i class="bi bi-pencil-square"></i> </a>
        <a asp-controller="Employee" asp-action="Index" asp-route-id="@employee. Id" class="btn btn-outline-danger"
           title="Delete"> <i class="bi bi-trash"></i> </a>

    } *@

</table>