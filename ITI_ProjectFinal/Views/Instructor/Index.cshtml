@model IEnumerable<Instructor>


<h1 class="alert alert-primary text-center m-3">

    Instructor Index
</h1

<div>
 <td>
     <a href="/Instructor/AddNew">   Create New </a>
 </td> 


</div>


@if(Model.Count() > 0) // captoin 
{
    <table class="table table-bordered table-hover caption-top text-center">
        <caption class="text-center fs-1">Instructors</caption>
        <thead class="table-dark">
            <tr>
                <th>@Html.DisplayNameFor(ins => ins.Id)</th>
                <th>@Html.DisplayNameFor(ins => ins.Name)</th>
                <th>@Html.DisplayNameFor(ins => ins.Address)</th>
                <th>@Html.DisplayNameFor(ins => ins.Salary)</th>
                <th>@Html.DisplayNameFor(ins => ins.ImageUrl)</th>
           
                                    
            </tr>
        </thead>
        <tbody>
            @foreach (var instructor in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(m => instructor.Id)</td>
                    <td>@Html.DisplayFor(m => instructor.Name)</td>
                    <td>@Html.DisplayFor(m => instructor.Address)</td>
                    <td>@Html.DisplayFor(m => instructor.Salary)</td>

                    <th>  <img src="@instructor.ImageUrl" alt="Instructor image" style="object-fit:contain" class="col-4" /> </th>
                  
                  

                    <td> <a href="/Instructor/Details/@instructor.Id" >Details</a> </td>

                   @*  <td> // Mix  use asp- (it auto complete if use strong type )
                        <a asp-controller="Employees" asp-action="GetDetailsView" asp-route-id="@employee.Id" class="btn btn-outline-primary" title="Details">
                            <i class="bi bi-list-ul"></i>
                        </a>
                        <a asp-controller="Employees" asp-action="GetEditView" asp-route-id="@employee.Id" class="btn btn-outline-secondary" title="Edit">
                            <i class="bi bi-pencil-square"></i>
                        </a>
                        <a asp-controller="Employees" asp-action="GetDeleteView" asp-route-id="@employee.Id" class="btn btn-outline-danger" title="Delete">
                            <i class="bi bi-trash"></i>
                        </a>
                    </td> *@
                </tr>
            }
        </tbody>
        <tfoot>
            <tr class="table-dark">
               
            </tr>
        </tfoot>
    </table>
    }